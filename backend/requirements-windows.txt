# Core
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
httpx>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
orjson
websockets

# ASR
openai-whisper
torch
torchaudio
transformers
imageio-ffmpeg

# Optional / local clients
sounddevice

# Notes
# - Coqui TTS is not currently available in this Windows Python 3.12 environment.
# - The runtime will continue to work with Piper if configured, or tone fallback otherwise.
